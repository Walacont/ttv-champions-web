{
  "name": "sc-champions-storage",
  "version": "1.0.0",
  "description": "Cloudflare Worker for SC Champions file storage with R2",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
