{
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "firebase-admin": "^13.6.0",
    "happy-dom": "^20.0.10",
    "vitest": "^4.0.8"
  }
}
